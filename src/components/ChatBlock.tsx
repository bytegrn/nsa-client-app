"use client";

export default function ChatBlock() {
  // fetch conversation history
  // if no history render the chat quick actions block
  // if chat history render the chat histroy in the chatsContainer block

  //

  return (
    <div>
      <div></div>
      <div></div>
    </div>
  );
}
